<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!--
        The Room database is the playlist library: the URLs the user added by hand and the
        only app state that cannot be rebuilt from the audio folder. It is included in
        backups so an uninstall or a device migration does not lose it.

        It was previously excluded, from when the database held download history and a
        fresh install was better off empty. That is no longer what it stores.
    -->
</full-backup-content>
